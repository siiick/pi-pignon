[project]
name = "laya-worker"
version = "0.2.0"
description = "Long-lived stdio worker for the Laya System-1 decision model (laya-mlx)"
requires-python = ">=3.11"
dependencies = ["laya-mlx>=0.1.0"]

[tool.uv]
package = false